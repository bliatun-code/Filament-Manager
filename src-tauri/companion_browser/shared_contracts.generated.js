// @generated by `npm run generate:shared-contracts`; do not edit.
// Source: src/backend/shared_contracts.rs

export const SPOOL_STATUSES = Object.freeze(["IN_STOCK","ASSIGNED","BORROWED","EMPTY","LOST","MISSING","DELETED"]);
const SPOOL_STATUSES_SET = new Set(SPOOL_STATUSES);
export function isCanonicalSpoolStatus(value) {
  return SPOOL_STATUSES_SET.has(value);
}

export const OWNERSHIP_TYPES = Object.freeze(["OWNED","BORROWED_IN"]);
const OWNERSHIP_TYPES_SET = new Set(OWNERSHIP_TYPES);
export function isCanonicalOwnershipType(value) {
  return OWNERSHIP_TYPES_SET.has(value);
}

export const LOAN_DIRECTIONS = Object.freeze(["OUTBOUND","INBOUND"]);
const LOAN_DIRECTIONS_SET = new Set(LOAN_DIRECTIONS);
export function isCanonicalLoanDirection(value) {
  return LOAN_DIRECTIONS_SET.has(value);
}

export const LOAN_STATUSES = Object.freeze(["ACTIVE","RETURNED","LOST","CANCELLED"]);
const LOAN_STATUSES_SET = new Set(LOAN_STATUSES);
export function isCanonicalLoanStatus(value) {
  return LOAN_STATUSES_SET.has(value);
}

function isContractObject(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}

export function isLowStockMaterialOverride(value) {
  return (
    isContractObject(value) &&
    typeof value.material_key === "string" &&
    typeof value.material === "string" &&
    Number.isSafeInteger(value.threshold_g)
  );
}

export function isLowStockPolicy(value) {
  return (
    isContractObject(value) &&
    Number.isSafeInteger(value.default_threshold_g) &&
    Array.isArray(value.material_overrides) && value.material_overrides.every((item) => isLowStockMaterialOverride(item))
  );
}
